import React from "react";

const OldHeader = () => (
  <header style={{ padding: "20px", backgroundColor: "#282c34", color: "white" }}>
    <h1>Old Header</h1>
    <p>This is the old header with minimal design.</p>
  </header>
);

export default OldHeader;
